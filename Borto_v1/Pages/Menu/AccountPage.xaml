<Page x:Class="Borto_v1.AccountPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Borto_v1" xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes" xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="900"
      Title="AccountPage"
      Unloaded="AccountPage_Unloaded"
     >
    <Page.Resources>
        <local:ParametrizedBooleanToVisibilityConverter x:Key="ParametrizedBooleanToVisibilityConverter"/>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <local:ByteToBitmapImageConverter x:Key="ByteToBitmapImageConverter"/>
    </Page.Resources>
    <Grid Background="{StaticResource ForegroundColorLight}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <materialDesign:ColorZone materialDesign:ShadowAssist.ShadowDepth="Depth2"
                                            Mode="PrimaryMid" >
            <StackPanel Background="{StaticResource BackgroundLightBlueBrush}" Orientation="Horizontal">
                <TextBlock  Text="Account Page" Margin="10" FontSize="22" FontWeight="Medium" Foreground="{StaticResource ForegroundColorLight}"/>
            </StackPanel>
        </materialDesign:ColorZone>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid Margin="5">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Border  Width="250" Height="250" CornerRadius="125" Margin="20 20 20 0">
                    <Border.Effect>
                        <DropShadowEffect ShadowDepth="5"/>
                    </Border.Effect>
                    <Border.Background>
                        <ImageBrush x:Name="AccountImage" ImageSource="{Binding Image,Mode=TwoWay,Converter={StaticResource ByteToBitmapImageConverter}}"/>
                    </Border.Background>
                </Border>
                <StackPanel  Orientation="Horizontal" VerticalAlignment="Top" HorizontalAlignment="Right" Grid.Row="1" Margin="20 5 20 10">
                    <Label  Content="Select a picture for your icon" FontSize="16" FontWeight="Medium" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    <Button 
                        IsEnabled="{Binding IsVisibleEditNameIcon, Converter={StaticResource InverseBooleanConverter}}"
                        Grid.Column="1" HorizontalAlignment="Right" 
                        Command="{Binding SetPathtoImageCommand}"
                                Style="{StaticResource MaterialDesignFloatingActionLightButton}"
                                >
                        <materialDesign:PackIcon Kind="Add" Height="35" Width="35" />
                    </Button>
                </StackPanel>
            </Grid>
            <Grid Margin="0 20" Grid.Row="0" Grid.Column="1">
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition Height="2*"/>
                    </Grid.RowDefinitions>
                    <Grid   Height="40" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.3*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <TextBlock Width="Auto" VerticalAlignment="Center" HorizontalAlignment="Center" Text="Name: " FontSize="20" />
                        <TextBox  Grid.Column="1" 
                                  x:Name="NameTextBox"
                                   VerticalAlignment="Center" 
                                  FontSize="20" MaxLength="20"
                                  IsReadOnly="{Binding IsVisibleEditNameIcon}" >
                            <TextBox.Text>
                                <Binding Path="Name" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <local:NotEmptyValidationRule  />
                                        <local:MinimumCharacterRule MinimumCharacters="5" />
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                    </Grid>
                    <Grid Grid.Row="1"  Height="40" >
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.3*"/>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" Text="Nickname: " FontSize="20" />
                        <TextBox  Grid.Column="1" 
                                  x:Name="NickNameTextBox"
                                   VerticalAlignment="Center" 
                                  FontSize="20" MaxLength="20"
                                   IsReadOnly="{Binding IsVisibleEditNameIcon}" >
                            <TextBox.Text>
                                <Binding Path="NickName" UpdateSourceTrigger="PropertyChanged">
                                    <Binding.ValidationRules>
                                        <local:NotEmptyValidationRule  />
                                        <local:MinimumCharacterRule MinimumCharacters="5" />
                                    </Binding.ValidationRules>
                                </Binding>
                            </TextBox.Text>
                        </TextBox>
                    </Grid>
                    <Grid Grid.Row="2">
                        <Button     Margin="0 0 10 0" Width="35" Height="35" Padding="0"
                                        Command="{Binding ChangeNameCommand}"
                                        Visibility="{Binding IsVisibleEditNameIcon,
                                        Converter={StaticResource ParametrizedBooleanToVisibilityConverter}}"
                                        >
                            <materialDesign:PackIcon Width="15" Height="15" Kind="Edit"  Foreground="{StaticResource ForegroundColorLight}"/>
                        </Button>
                        <StackPanel HorizontalAlignment="Center"  Visibility="{Binding IsVisibleEditNameIcon,
                                    Converter={StaticResource ParametrizedBooleanToVisibilityConverter},
                                    ConverterParameter=False}"  Orientation="Horizontal">
                            <Button Command="{Binding SaveChangeNameCommand}" 
                                    Height="35" Padding="0" Width="35"
                                    Foreground="{StaticResource ForegroundColorLight}"
                                     >
                                <Button.Content>
                                    <Grid>
                                        <materialDesign:PackIcon 
                                    Width="15" Height="15" Kind="Check"  Foreground="{StaticResource ForegroundColorLight}"/>

                                    </Grid>
                                </Button.Content>
                                <Button.Style >
                                    <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedLightButton}">
                                        <Setter Property="IsEnabled" Value="False"/>
                                        <Style.Triggers>
                                            <MultiDataTrigger>
                                                <MultiDataTrigger.Conditions>
                                                    <Condition Binding="{Binding Path=(Validation.HasError), ElementName=NameTextBox}" Value="False"/>
                                                    <Condition Binding="{Binding Path=(Validation.HasError), ElementName=NickNameTextBox}" Value="False"/>
                                                </MultiDataTrigger.Conditions>
                                                <Setter Property="IsEnabled" Value="True"/>
                                            </MultiDataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                            <Button Width="35" Height="35" Padding="0"
                                        Margin="20 0 0 0"
                                        Command="{Binding CancelNameCommand}">
                                <materialDesign:PackIcon Width="15" Height="15" Kind="Cancel"  Foreground="{StaticResource ForegroundColorLight}"/>
                            </Button>
                        </StackPanel>
                    </Grid>
                    <Grid  Grid.Row="3">
                        <Button VerticalAlignment="Top"
                                HorizontalAlignment="Center"
                                Content="Change Password" 
                                Command="{Binding ChangePasswordCommand}"
                                Style="{StaticResource MaterialDesignFlatLightBgButton}"
                                Visibility="{Binding IsVisibleEditPasswrodIcon,
                                    Converter={StaticResource ParametrizedBooleanToVisibilityConverter}}"
                                />
                        <Grid  Visibility="{Binding IsVisibleEditPasswrodIcon,
                                    Converter={StaticResource ParametrizedBooleanToVisibilityConverter},ConverterParameter=False}"
                               >
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>
                            <Grid   Height="40" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.3*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" HorizontalAlignment="Center"  VerticalAlignment="Center" Text="Old Password: " FontSize="20" />
                                <PasswordBox  Grid.Column="1" Name="OldPassword"  VerticalAlignment="Center" Padding="0"  MaxLength="20">
                                    <PasswordBox.Style>
                                        <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignFloatingHintPasswordBox}">
                                            <Style.Setters>
                                                <Setter Property="local:PasswordBoxAssistant.BindPassword" Value="True"/>
                                                <Setter Property="local:PasswordBoxAssistant.BoundPassword">
                                                    <Setter.Value>
                                                        <Binding Path="OldPassword" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                                            <Binding.ValidationRules>
                                                                <local:NotEmptyValidationRule  />
                                                                <local:MinimumCharacterRule MinimumCharacters="5" />
                                                            </Binding.ValidationRules>
                                                        </Binding>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style.Setters>
                                        </Style>
                                    </PasswordBox.Style>
                                </PasswordBox>
                            </Grid>
                            <Grid  Grid.Row="1"  Height="40" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.3*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Text="New Password: " FontSize="20" />
                                <PasswordBox  Grid.Column="1" x:Name="NewPassword"  VerticalAlignment="Center" Padding="0"  MaxLength="20">
                                    <PasswordBox.Style>
                                        <Style TargetType="PasswordBox" BasedOn="{StaticResource MaterialDesignFloatingHintPasswordBox}">
                                            <Style.Setters>
                                                <Setter Property="local:PasswordBoxAssistant.BindPassword" Value="True"/>
                                                <Setter Property="local:PasswordBoxAssistant.BoundPassword">
                                                    <Setter.Value>
                                                        <Binding Path="NewPassword" UpdateSourceTrigger="PropertyChanged" Mode="TwoWay">
                                                            <Binding.ValidationRules>
                                                                <local:NotEmptyValidationRule  />
                                                                <local:MinimumCharacterRule MinimumCharacters="5" />
                                                            </Binding.ValidationRules>
                                                        </Binding>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style.Setters>
                                        </Style>
                                    </PasswordBox.Style>
                                </PasswordBox>
                            </Grid>
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="2">
                                <Button Command="{Binding SaveChangePasswordCommand}" 
                                    Height="35" Padding="0" Width="35"
                                    Foreground="{StaticResource ForegroundColorLight}"
                                     >
                                    <Button.Content>
                                        <Grid>
                                            <materialDesign:PackIcon 
                                    Width="15" Height="15" Kind="Check"  Foreground="{StaticResource ForegroundColorLight}"/>

                                        </Grid>
                                    </Button.Content>
                                    <Button.Style >
                                        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedLightButton}">
                                            <Setter Property="IsEnabled" Value="False"/>
                                            <Style.Triggers>
                                                <MultiDataTrigger>
                                                    <MultiDataTrigger.Conditions>
                                                        <Condition Binding="{Binding Path=(Validation.HasError), ElementName=OldPassword}" Value="False"/>
                                                        <Condition Binding="{Binding Path=(Validation.HasError), ElementName=NewPassword}" Value="False"/>
                                                    </MultiDataTrigger.Conditions>
                                                    <Setter Property="IsEnabled" Value="True"/>
                                                </MultiDataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <Button Width="35" Height="35" Padding="0"
                                        Margin="20 0 0 0"
                                        Command="{Binding CancelPasswordCommand}">
                                    <materialDesign:PackIcon Width="15" Height="15" Kind="Cancel"  Foreground="{StaticResource ForegroundColorLight}"/>
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Grid>
            </Grid>
            <Grid Grid.Row="1" Grid.ColumnSpan="2"  VerticalAlignment="Bottom">
                <StackPanel>
                    <TextBlock Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                           Text="My Videos"/>
                    <Border Height="Auto" BorderBrush="{StaticResource BackgroundLightBlueBrush}" BorderThickness="1" Margin="3" >
                        <ScrollViewer HorizontalScrollBarVisibility="Visible" VerticalScrollBarVisibility="Disabled"
              PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
                            <ListBox  x:Name="ListViewProduct" ScrollViewer.VerticalScrollBarVisibility="Disabled" Background="{StaticResource ForegroundColorLight}" ItemsSource="{Binding Videos}" ItemContainerStyle="{DynamicResource ListBoxItemStyle1}" >
                                <ListBox.ItemTemplate>
                                    <DataTemplate >
                                        <Border   Name="border" Margin="20 5 20 5" Width="160" Height="150">
                                            <StackPanel >
                                                <Border  Width="150" Height="90" CornerRadius="5" Margin="5">
                                                    <Border.Effect>
                                                        <DropShadowEffect ShadowDepth="5"/>
                                                    </Border.Effect>
                                                    <Border.Background>
                                                        <ImageBrush ImageSource="{Binding Image, Mode=OneTime}"/>
                                                    </Border.Background>
                                                </Border>
                                                <Grid>
                                                    <StackPanel Margin="3">
                                                        <TextBlock Margin="5" Text="{Binding Name, Mode=OneTime}" 
                                                           FontSize="14"
                                                           TextTrimming="CharacterEllipsis"
                                                           FontFamily="Franklin Gothic Medium" 
                                                           Width="100" HorizontalAlignment="Left" />
                                                    </StackPanel>
                                                    <Button  HorizontalAlignment="Right" Margin="0 0 5 0" Padding="5"
                                        Command="{Binding ElementName= ListViewProduct,Path=DataContext.DownloadPageCommand}" 
                                        CommandParameter="{Binding Path=SelectedItem , ElementName=ListViewProduct}">
                                                        <materialDesign:PackIcon Width="12" Height="10" Kind="Edit" Foreground="{StaticResource ForegroundColorLight}"/>
                                                    </Button>
                                                </Grid>
                                            </StackPanel>
                                            <i:Interaction.Triggers>
                                                <i:EventTrigger EventName="MouseLeftButtonUp">
                                                    <i:InvokeCommandAction Command="{Binding ElementName= ListViewProduct,Path=DataContext.ViewWatchingPageCommand}" 
                                                       CommandParameter="{Binding Path=SelectedItem , ElementName=ListViewProduct}"/>
                                                </i:EventTrigger>
                                            </i:Interaction.Triggers>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                            </ListBox>
                        </ScrollViewer>
                    </Border>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</Page>
